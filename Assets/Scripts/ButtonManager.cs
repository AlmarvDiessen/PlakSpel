using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonManager : MonoBehaviour
{
    public List<bool> allButtons;

    private void Update()
    {
        if (allButtons.Count >= 4)
        {
            // luik kapot
        }
    }
}
